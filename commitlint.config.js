const Configuration = {
    // https://github.com/vivaxy/vscode-conventional-commits#supported-commitlint-rules
    rules: {
        'type-enum': [2, 'always', ['this.is.from.commitlint.config.js','2']],
    },
}

module.exports = Configuration;